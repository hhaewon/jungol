m_{i} 합이 M 이상 되는 것 중 c_{i} 합이 최소인거

dp[i][j] : i까지 사용해서 메모리 j를 만들때 최소 비용



j+b[i] >= M : ans = min(ans, dp[i-1][j]+c[i])

M <= 10000000인데?? 

풀이 봤는데
dp[i][j]를 i까지 사용했을 때 비용 j로 만들 수 있는 최대 메모리
로 하면 된다고 한다.